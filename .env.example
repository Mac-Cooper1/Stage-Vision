# Stager Agent - Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# Google API Configuration (Required)
# =============================================================================

# Get your API key from: https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Optional: Override default API base URL
# GEMINI_API_BASE_URL=https://generativelanguage.googleapis.com/v1beta

# Override model IDs
GEMINI_VISION_MODEL=gemini-3-pro-preview
GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview

# =============================================================================
# SMTP Email Configuration (Required for delivery)
# =============================================================================

# Gmail example:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=

# For Gmail, you need to use an App Password:
# 1. Enable 2FA on your Google account
# 2. Go to https://myaccount.google.com/apppasswords
# 3. Create an app password for "Mail"

# From address displayed in emails
EMAIL_FROM=Stage Vision <no-reply@44frames.com>


# =============================================================================
# Airtable config
# =============================================================================
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
AIRTABLE_TABLE_NAME=

# =============================================================================
# Optional Configuration
# =============================================================================

# Base directory for job folders (default: ./stager_jobs)
# BASE_JOBS_DIR=./stager_jobs

# API request timeout in seconds (default: 120)
# REQUEST_TIMEOUT=120

# Max retries for API calls (default: 3)
# MAX_RETRIES=3
